<h1>Football team score manager</h1>

## Instalation
```
cp .env.example .env
sail composer install
sail build --no-cache
docker-compose down -v
sail up --build -d
sail npm install
sail npm run dev
```
